# Database
# Neon pooled connection string (for application runtime)
DATABASE_URL="postgresql://user:password@host.neon.tech/calmill?sslmode=require"

# Neon direct connection string (for Prisma migrations and CLI operations)
DIRECT_DATABASE_URL="postgresql://user:password@host.neon.tech/calmill?sslmode=require"

# Auth
# Generate with: openssl rand -base64 32
AUTH_SECRET="your-auth-secret-here"

# Base URL for NextAuth
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth (optional)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Email (Resend - optional)
RESEND_API_KEY=""
EMAIL_FROM="CalMill <noreply@calmill.workermill.com>"

# App
# Public URL for the application
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Seed token for demo data endpoint
SEED_TOKEN="calmill-seed-token-dev"
